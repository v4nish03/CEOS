import 'package:ceos/core/widgets/work_in_progress_view.dart';
import 'package:flutter/material.dart';

class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const WorkInProgressView(
      title: 'Login',
      description:
          'La vista de login fue removida temporalmente para reiniciar el módulo móvil. Usa esta pantalla como base para la nueva implementación.',
    );
  }
}
